<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pk.edu.pucit.mcproject">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".show_uploaded_videos" />
        <activity android:name=".Show_Uploaded_Images" />
        <activity android:name=".SignUpActivity"/>
        <activity android:name=".LogInActivity" />

        <activity
            android:name=".SelectedCategory"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".PlaceDetailActivity" />
        <activity android:name=".City_details" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Add_Image_Fragment" />
        <activity android:name=".Add_Video" />
    </application>

</manifest>